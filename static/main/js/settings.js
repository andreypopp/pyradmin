(function () {

var settings = {
	pageSize: 20,

	paths: {
		stat: '/static',
		api: '/',
		models: '/',
		templates: '/static/main/templates'
	}
};

window.pyradmin = window.pyradmin || {};
window.pyradmin.settings = settings;

})();
